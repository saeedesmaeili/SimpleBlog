@model DateTime?

@{
    string persianDate = Blog.Helpers.PersianDateExtension.GetPersianDateTimeString(Model);
    string latinDate = "";
    if (Model != null)
    {
        latinDate = ((DateTime)Model).ToString("yyyy dd MMM HH:mm:ss");
    }
}


@persianDate